DECLARE
	NUM NUMBER;
BEGIN
	SELECT SALARY INTO NUM FROM EMPLOYEES WHERE EMPLOYEE_ID=100;
	DBMS_OUTPUT.PUT_LINE(NUM);
END;

DECLARE
	NUM2 NUMBER;
BEGIN
	SELECT SALARY INTO NUM2 FROM EMPLOYEES WHERE EMPLOYEE_ID=99;
	DBMS_OUTPUT.PUT_LINE(NUM2);
END;